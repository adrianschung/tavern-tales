<div class="container col-12 col-lg-6">
  <h1>Users</h1>
  <%= will_paginate @users %>
  <% @users.each do |user| %>
    <div class="user-box">
      <h3><%= user.username %></h3>
    </div>
  <% end %>
  <%= will_paginate @users %>
</div>